#include<bits/stdc++.h>
using namespace std;


int main(int argc, char const *argv[])
{
    // this is used for fast input output so that the buffer doesn't flush the output each time
    // incase of interactive problems do not use this or use endl along with this
    ios_base::sync_with_stdio(NULL); cin.tie(NULL); cout.tie(NULL);


    cout<<12<<'\n'; //stores the output in the buffer
    cout<<4<<endl; // stores the output and imediately flushes the output to console
                    // cout<<endl;  ==   cout<<'\n'<<flush;
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        // cout<<"this is n "<<n<<'\n';
        cout<<"this is n "<<n<<endl;
    }
    

    

    return 0;

}